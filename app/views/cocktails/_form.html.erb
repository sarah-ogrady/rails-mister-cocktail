<div class="container better-form">
  <h1>Add a new cocktail</h1>
  <%= simple_form_for(cocktail) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file, class: "btn btn-primary"  %>
    <%= f.submit class: "btn btn-primary"   %>
  <% end %>
</div>
